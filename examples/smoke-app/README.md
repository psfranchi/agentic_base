# smoke-app

Toy app for **agentic_base** adopt dry-run. Not a product.

Use: copy next to `agentic_base` as `../smoke-app`, then run `adopt-base` into that path. See [`docs/adopt-dry-run.md`](../../docs/adopt-dry-run.md).
